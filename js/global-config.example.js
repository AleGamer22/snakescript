// Copy this file to `js/global-config.js` and fill with your Firebase Web App config.
// Steps:
// 1. Create a Firebase project at https://console.firebase.google.com
// 2. Create a Web App under Project settings -> General
// 3. Enable Firestore (in test mode for quick setup) in Build -> Firestore Database
// 4. Copy the firebase config below and paste into `js/global-config.js` as window.GLOBAL_LEADERBOARD_CONFIG

window.GLOBAL_LEADERBOARD_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
